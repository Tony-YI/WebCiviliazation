//var result_list; global variable

function update_move(from_x, from_y, to_x, to_y)
{

}

function update_slot_own() //TODO: update the slot every movement
{
	while(var result = result_list.pop())
	{
		if(result.player_id == user_id) //user_id: global variable in display.js
		{

		}
	}
}